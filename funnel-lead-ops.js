/* SHEEHY SALES HQ - Funnel reachability, distance, video delivery and attempt log */
(function(g){
'use strict';
var LOG_KEY='shq_lead_log_v1',STORE_ZIP='20110';
var state={channels:{phone:true,email:true,text:false},leadAt:'',distanceMiles:null,distanceFar:false,distanceState:'idle'};
var zipCache={},storePromise=null,zipTimer=null;
function $(id){return document.getElementById(id);}
function field(name,fb){var e=document.querySelector('[data-f="'+name+'"]'),v=e&&typeof e.value==='string'?e.value.trim():'';return v||fb;}
function stage(){var e=$('stageSelect');return e?e.value:'';}
function behavior(){var e=$('behavior');return e?e.value:'';}
function scenario(id){var a=(g.SHQFunnel&&g.SHQFunnel.scenarios)||[];for(var i=0;i<a.length;i++)if(a[i].id===id)return a[i];return null;}
function context(){var c={phone:!!state.channels.phone,email:!!state.channels.email,text:!!state.channels.text,stage:stage(),scenario:behavior(),distanceMiles:state.distanceMiles,distanceFar:!!state.distanceFar,distanceState:state.distanceState};c.emailOnly=c.email&&!c.phone&&!c.text;c.textOnly=c.text&&!c.phone&&!c.email;c.phoneOnly=c.phone&&!c.email&&!c.text;return c;}
function notify(){try{g.dispatchEvent(new CustomEvent('shq:funnel-context-change',{detail:context()}));}catch(e){}}
g.SHQFunnelExecution={getContext:context};
function esc(s){return String(s==null?'':s).replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
function fill(text){var m={'[Name]':field('name','[Name]'),'[vehicle]':field('vehicle','[vehicle]'),'[agent]':field('agent','[agent]'),'[number]':field('number','[number]')};var out=String(text||'');Object.keys(m).forEach(function(k){out=out.split(k).join(m[k]);});return out;}
function modeText(){var c=context();if(c.emailOnly)return 'Email only';if(c.phoneOnly)return 'Phone only';if(c.textOnly)return 'Text only';var a=[];if(c.phone)a.push('Phone');if(c.text)a.push('Text');if(c.email)a.push('Email');return a.join(' + ')||'No channel';}
function renderChannels(){var box=$('channelChips');if(!box)return;box.innerHTML='';[['phone','Phone'],['text','Text OK'],['email','Email']].forEach(function(d){var b=document.createElement('button');b.type='button';b.className='channel-chip'+(state.channels[d[0]]?' on':'');b.setAttribute('data-channel',d[0]);b.textContent=d[1];b.onclick=function(){state.channels[d[0]]=!state.channels[d[0]];renderChannels();renderDelivery();notify();};box.appendChild(b);});var pill=$('contactMode');if(pill)pill.textContent=modeText();}
function age(){var p=$('leadAge');if(!p)return;p.className='status-pill';if(!state.leadAt){p.textContent='Not set';return;}var d=new Date(state.leadAt);if(isNaN(d)){p.textContent='Invalid time';return;}var m=Math.max(0,Math.round((Date.now()-d.getTime())/60000));if(m<=15){p.classList.add('good');p.textContent=m+'m old';}else if(m<=120){p.classList.add('blue');p.textContent=Math.max(1,Math.round(m/60))+'h old';}else if(m<=1440){p.classList.add('warn');p.textContent=Math.max(1,Math.round(m/60))+'h old';}else{p.classList.add('bad');p.textContent=Math.max(1,Math.round(m/1440))+'d old';}}
function radians(x){return x*Math.PI/180;}
function miles(a,b){var R=3958.7613,dLat=radians(b.lat-a.lat),dLon=radians(b.lon-a.lon),q=Math.sin(dLat/2)*Math.sin(dLat/2)+Math.cos(radians(a.lat))*Math.cos(radians(b.lat))*Math.sin(dLon/2)*Math.sin(dLon/2);return 2*R*Math.atan2(Math.sqrt(q),Math.sqrt(1-q));}
function zipPoint(zip){if(zipCache[zip])return Promise.resolve(zipCache[zip]);return fetch('https://api.zippopotam.us/us/'+encodeURIComponent(zip)).then(function(r){if(!r.ok)throw new Error('zip');return r.json();}).then(function(j){var p=j&&j.places&&j.places[0];if(!p)throw new Error('zip');var v={lat:parseFloat(p.latitude),lon:parseFloat(p.longitude)};zipCache[zip]=v;return v;});}
function storePoint(){if(!storePromise)storePromise=zipPoint(STORE_ZIP);return storePromise;}
function renderDistance(){var p=$('distanceStatus');if(!p)return;p.className='status-pill';if(state.distanceState==='loading'){p.classList.add('blue');p.textContent='Checking ZIP…';return;}if(state.distanceState==='error'){p.classList.add('bad');p.textContent='Distance unavailable';return;}if(state.distanceMiles==null){p.textContent='Add ZIP for distance';return;}var n=Math.round(state.distanceMiles);if(state.distanceFar){p.classList.add('warn');p.textContent='Long-distance · ~'+n+' mi by ZIP';}else{p.classList.add('good');p.textContent='Regional · ~'+n+' mi by ZIP';}}
function checkZip(){clearTimeout(zipTimer);zipTimer=setTimeout(function(){var z=field('zipcode','').replace(/\D/g,'').slice(0,5);if(z.length!==5){state.distanceMiles=null;state.distanceFar=false;state.distanceState='idle';renderDistance();notify();return;}state.distanceState='loading';renderDistance();Promise.all([storePoint(),zipPoint(z)]).then(function(v){state.distanceMiles=miles(v[0],v[1]);state.distanceFar=state.distanceMiles>100;state.distanceState='ready';renderDistance();notify();}).catch(function(){state.distanceMiles=null;state.distanceFar=false;state.distanceState='error';renderDistance();notify();});},350);}
function opts(sel,items,wanted){sel.innerHTML='';items.forEach(function(v){var o=document.createElement('option');o.value=v;o.textContent=v==='sms'?'SMS':'Email';sel.appendChild(o);});if(items.indexOf(wanted)>-1)sel.value=wanted;}
function behaviorVariant(){var F=g.SHQFunnel,c=context();return F&&typeof F.getChannelVariant==='function'?F.getChannelVariant(behavior(),c):null;}
function followupFocus(id){var m={
'availability-first':'I sent it so you can see the exact vehicle context I am checking for you.',
'price-first':'I sent it so we are both looking at the same vehicle while I work the comparison you asked for.',
'test-drive-request':'I sent it so you can see the vehicle before we lock the test-drive time.',
'wants-details':'I sent it around the specific detail you asked about, not a generic walkaround.',
'payment-apr':'I sent it so you can see the vehicle while I keep the finance answer accurate instead of guessing.',
'trade-value':'I sent it so you can see the vehicle you are considering before we handle the appraisal on your trade.',
'credit-concern':'I kept the video focused on the vehicle and your next step stays private.',
'competitor-shop':'I sent it so you have something real to compare with the other option you are considering.',
'decision-maker':'I sent it so both decision makers can see the same vehicle and the same information.',
'unit-gone':'I sent a quick video update on the requested vehicle and the closest verified direction I found.',
'no-response-day1':'I kept it short so you can tell me whether you still need vehicle status, numbers or a time to see it.',
'final-nudge':'I kept it to one quick update so you can tell me whether you are still looking or already handled it.'};return m[id]||'I sent it so you can see the vehicle and exactly what I am working on for you.';}
function distanceFollowup(){var id=behavior();if(id==='unit-gone')return 'Since you are coming from a good distance away, I will verify the alternative you actually want to see before you head this way.';if(id==='availability-first'||id==='test-drive-request')return 'Since you are coming from a good distance away, I will re-verify the exact vehicle before you head this way.';if(id==='price-first')return 'Since you are coming from a good distance away, I will make sure the vehicle and comparison are verified before you make the trip.';return 'Since you are coming from a good distance away, I will verify the relevant vehicle details before you head this way.';}
function genericVideoNotice(method){var f=followupFocus(behavior());if(method==='email')return 'Subject: 🎥 '+fill('[Name]')+', Quick Video on the '+fill('[vehicle]')+'\n\nHi '+fill('[Name]')+',\n\nI just sent you a quick video on the '+fill('[vehicle]')+'. '+f+'\n\nDid it come through?\n\n'+fill('[agent]');return 'Hi '+fill('[Name]')+', I just sent you a quick video on the '+fill('[vehicle]')+'. '+f+' Did it come through?';}
function videoNotice(method){var c=context(),v=behaviorVariant(),base;if(method==='email'&&c.emailOnly&&v&&v.videoNoticeEmail)base='Subject: '+fill(v.videoNoticeSubject||'🎥 [Name], Quick Video on the [vehicle]')+'\n\n'+fill(v.videoNoticeEmail);else base=genericVideoNotice(method);if(c.distanceFar&&base)base+='\n\n'+distanceFollowup();return base;}
function renderDelivery(){var host=$('videoDeliveryControls'),notice=$('videoNotice'),c=context();if(!host||!notice)return;var methods=[];if(c.email)methods.push('email');if(c.text)methods.push('sms');if(!methods.length){host.innerHTML='<div class="empty-channels">No digital video-delivery channel is available. Enable Email or Text OK in Customer Context.</div>';notice.textContent='';['copyNotice','logVideo','logNotice'].forEach(function(id){if($(id))$(id).disabled=true;});return;}['copyNotice','logVideo','logNotice'].forEach(function(id){if($(id))$(id).disabled=false;});host.innerHTML='<div class="delivery-grid"><label>Video sent by<select id="videoVia"></select></label><label>Follow-up by<select id="notifyVia"></select></label></div>';var vv=$('videoVia'),nv=$('notifyVia'),defaultVideo=c.emailOnly?'email':(c.text&&!c.email?'sms':methods[0]);opts(vv,methods,defaultVideo);opts(nv,methods,methods.length>1?(vv.value==='email'?'sms':'email'):vv.value);if(methods.length>1&&nv.value===vv.value)nv.value=vv.value==='email'?'sms':'email';function refresh(){if(methods.length>1&&nv.value===vv.value)nv.value=vv.value==='email'?'sms':'email';notice.textContent=videoNotice(nv.value);}vv.onchange=refresh;nv.onchange=refresh;refresh();}
function currentFollowup(){var nv=$('notifyVia');return nv?videoNotice(nv.value):'';}
function currentVideoMethod(){var vv=$('videoVia');return vv?vv.value:'';}
function currentFollowupMethod(){var nv=$('notifyVia');return nv?nv.value:'';}
function fallback(t){var a=document.createElement('textarea');a.value=t;a.style.position='fixed';a.style.opacity='0';document.body.appendChild(a);a.select();try{document.execCommand('copy');}catch(e){}a.remove();}
function copy(t,b){function done(){var o=b.textContent;b.textContent='Copied';setTimeout(function(){b.textContent=o;},900);}if(navigator.clipboard&&navigator.clipboard.writeText)navigator.clipboard.writeText(t).then(done).catch(function(){fallback(t);done();});else{fallback(t);done();}}
function load(){try{return JSON.parse(localStorage.getItem(LOG_KEY)||'[]');}catch(e){return [];}}
function save(a){try{localStorage.setItem(LOG_KEY,JSON.stringify(a));}catch(e){}}
function addLog(what,res){var a=load();a.push({id:'l'+Date.now(),who:field('name',''),what:what,res:res||'No response yet',at:new Date().toISOString()});if(a.length>100)a=a.slice(-100);save(a);renderLog();}
function renderLog(){var b=$('leadLog'),sum=$('attemptSummary');if(!b||!sum)return;var all=load(),a=all.slice().reverse().slice(0,8);sum.textContent='Attempt log'+(all.length?' · '+all.length+' saved':'');if(!a.length){b.innerHTML='<div class="quiet" style="padding-top:9px">No attempts logged on this browser yet.</div>';return;}b.innerHTML='';a.forEach(function(e){var r=document.createElement('div'),d=new Date(e.at);r.className='logitem';r.innerHTML='<div><b>'+esc(e.who||'Lead')+'</b> · '+esc(e.what)+'<div class="logmeta">'+esc(e.res)+' · '+d.toLocaleDateString('en-US',{month:'short',day:'numeric'})+' '+d.toLocaleTimeString('en-US',{hour:'numeric',minute:'2-digit'})+'</div></div>';var x=document.createElement('button');x.className='logrm';x.textContent='Remove';x.onclick=function(){save(load().filter(function(q){return q.id!==e.id;}));renderLog();};r.appendChild(x);b.appendChild(r);});}
function bind(){renderChannels();renderDistance();renderDelivery();renderLog();var at=$('leadAt');if(at)at.oninput=function(){state.leadAt=this.value;age();};if($('leadNow'))$('leadNow').onclick=function(){var d=new Date(),z=d.getTimezoneOffset()*60000;at.value=new Date(d-z).toISOString().slice(0,16);state.leadAt=at.value;age();};if($('customerZip'))$('customerZip').addEventListener('input',checkZip);if($('copyNotice'))$('copyNotice').onclick=function(){copy(currentFollowup(),this);};if($('logVideo'))$('logVideo').onclick=function(){var m=currentVideoMethod();if(m)addLog('Sent video by '+(m==='sms'?'SMS':'email'),'No response yet');};if($('logNotice'))$('logNotice').onclick=function(){var m=currentFollowupMethod();if(m)addLog('Video follow-up by '+(m==='sms'?'SMS':'email'),'No response yet');};if($('logAdd'))$('logAdd').onclick=function(){addLog($('logWhat').value,$('logResult').value);};g.addEventListener('shq:funnel-state-change',function(){renderDelivery();notify();});g.addEventListener('shq:funnel-customer-cleared',function(){state.channels={phone:true,email:true,text:false};state.leadAt='';if($('leadAt'))$('leadAt').value='';state.distanceMiles=null;state.distanceFar=false;state.distanceState='idle';renderChannels();age();renderDistance();renderDelivery();notify();});document.addEventListener('input',function(e){if(e.target&&e.target.matches&&e.target.matches('[data-f]')&&e.target.id!=='customerZip')setTimeout(renderDelivery,0);});setInterval(age,30000);checkZip();notify();}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind);else bind();
})(window);